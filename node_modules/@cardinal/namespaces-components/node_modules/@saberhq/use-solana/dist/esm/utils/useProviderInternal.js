import { DEFAULT_PROVIDER_OPTIONS, SolanaAugmentedProvider, SolanaProvider, SolanaReadonlyProvider, } from "@saberhq/solana-contrib";
import { useMemo } from "react";
export const useProviderInternal = ({ connection, sendConnection = connection, wallet, commitment = "confirmed", confirmOptions = DEFAULT_PROVIDER_OPTIONS, }) => {
    const provider = useMemo(() => new SolanaReadonlyProvider(connection, {
        commitment,
    }), [commitment, connection]);
    const connected = wallet === null || wallet === void 0 ? void 0 : wallet.connected;
    const publicKey = wallet === null || wallet === void 0 ? void 0 : wallet.publicKey;
    const providerMut = useMemo(() => wallet && connected && publicKey
        ? new SolanaAugmentedProvider(SolanaProvider.load({
            connection,
            sendConnection,
            wallet: wallet,
            opts: confirmOptions,
        }))
        : null, [wallet, connected, publicKey, connection, sendConnection, confirmOptions]);
    return {
        provider,
        providerMut,
    };
};
//# sourceMappingURL=useProviderInternal.js.map