import { PublicKey } from "@solana/web3.js";
/**
 * Finds the group reward entry id.
 * @returns
 */
export declare const findGroupRewardEntryId: (groupRewardDistributorId: PublicKey, groupEntryId: PublicKey) => PublicKey;
/**
 * Finds the group reward entry id.
 * @returns
 */
export declare const findGroupRewardCounterId: (groupRewardDistributorId: PublicKey, authority: PublicKey) => PublicKey;
/**
 * Finds the group reward distributor id.
 * @returns
 */
export declare const findGroupRewardDistributorId: (id: PublicKey) => PublicKey;
//# sourceMappingURL=pda.d.ts.map